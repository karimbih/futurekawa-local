DATABASE_URL=postgresql+psycopg2://futurekawa:futurekawa@localhost:5433/futurekawa_local

# Clé API partagée avec le backend central (en-tête X-API-Key)
API_KEY=change-me

# Configuration SMTP pour l'envoi des alertes par e-mail
SMTP_SERVER=smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=user@example.com
SMTP_PASSWORD=change-me
